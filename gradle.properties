# Allow Gradle Managed Devices below API 27 so the OS matrix can include the minSdk (24) floor.
# (Google flags this as experimental; if the API 24 image proves flaky on CI, raise the floor.)
android.experimental.testOptions.managedDevices.allowOldApiLevelDevices=true

# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx6144m -XX:MaxMetaspaceSize=1024m -Dfile.encoding=UTF-8 -XX:+HeapDumpOnOutOfMemoryError
# Limit parallel workers to reduce peak memory during D8 merge
org.gradle.workers.max=2
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
# AGP 9 ships built-in Kotlin support, which disallows adding Kotlin sources via the
# `kotlin.sourceSets` DSL. KSP still registers its generated sources that way, so allow it via
# this documented bridge flag until KSP migrates to the `android.sourceSets` DSL.
# See https://developer.android.com/r/tools/built-in-kotlin
android.disallowKotlinSourceSets=false
# Publishing: SDK
groupName=com.paydock
versionName=5.6.0
libraryName=mobile-sdk
projectGithubUrl=https://github.com/PayDock/android-mobile-sdk
projectDescription=The Paydock Mobile Android SDK provides an easy way to build and integrate with the Paydock orchestration platform for an Android app. We provide powerful and customizable UI elements that can be used out-of-the-box to collect your users' payment details. We also expose the low-level APIs that power those UIs so that you can build fully custom experiences.
# Publishing: Developer Info
developerId=john-paul-costello
developerName=John Paul Costello
developerEmail=john.costlelo@paydock.com
developerOrganisation=Paydock
developerOrganisationUrl=https://paydock.com/