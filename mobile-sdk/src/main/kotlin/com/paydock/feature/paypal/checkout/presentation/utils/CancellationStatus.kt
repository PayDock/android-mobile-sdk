package com.paydock.feature.paypal.checkout.presentation.utils

/**
 * Enum representing the different types of cancellation statuses for PayPal transactions.
 */
internal enum class CancellationStatus {
    /**
     * Represents a cancellation initiated by the user, typically when the user navigates back
     * or closes the payment process.
     */
    USER_INITIATED
}